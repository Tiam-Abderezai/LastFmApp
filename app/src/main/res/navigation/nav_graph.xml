<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_dest_albums_frag">

    <fragment
        android:id="@+id/nav_dest_albums_frag"
        android:name="com.example.lastfmapp.albums.AlbumListFragment"
        tools:layout="@layout/fragment_album_list">

        <action
            android:id="@+id/to_ArtistListFragment"
            app:destination="@id/nav_dest_artists_frag" />
    </fragment>
    <fragment
        android:id="@+id/nav_dest_artists_frag"
        android:name="com.example.lastfmapp.albums.ArtistListFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_artist_list">

        <action
            android:id="@+id/to_AlbumListFragment"
            app:destination="@id/nav_dest_albums_frag" />
    </fragment>
</navigation>